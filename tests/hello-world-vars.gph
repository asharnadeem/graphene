int a;
string b;
float y;
node<float> f;
edge<int> i;
graph<string> g;
list<node<int> > l;
int main() {
  string hw = "Hello World!";
  sprint(hw); 
  int x = 1;
  if(x == 0) {
    sprint("again");
  }
  else {
    sprint("not");
  }
}

void test(int x) {
  return;
}
