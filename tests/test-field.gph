int main() {
    node<int> n, m;
    list<edge<int>> l;
    graph<int> g;
    n.val = 1;
    n.id = 0;
    m.val = 2;
    m.id = 1;
    n <->[30] m;
    g.add_node(n);
    g.add_node(m);

   // print(m.edges[0].to);
    list<node<int>> ll;
    ll.push_back(n);
 
    //print(ll[0]);
    print(g.nodes[0].edges[0].to.val);
    print(n.edges[0].to.val);
    print(g.nodes[1].edges[0].to.val);
    print(m.edges[0].to.val); 
}